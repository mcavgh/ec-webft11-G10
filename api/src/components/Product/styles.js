import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    container: {
        maxWidth: 1100,
        margin: '0 auto'
    },
    media: {
        width: '100%',
    },
    info: {
        height: '100%',
    },
    button: {
        marginTop: 'auto'
    }
  });